/**
 * Undo for painted pixels.
 */

export type { PixelPatch, PixelRect, TilePatch } from './types';
export { MAX_TILES, TILE_SIZE } from './types';
export { dabRegion, tileKey, tilesCovering } from './tiles';
export { TileCollector } from './collector';
